# Nasaq Technical Documentation | المرجع التقني لـ "نسق"

هذا الملف مخصص للمطورين والمهندسين المهتمين بفهم البنية التحتية لتطبيق "نسق".

## 🏗️ التصميم المعماري (Architecture)
يعتمد "نسق" على مبدأ **Local-First**، حيث تظل البيانات ملكاً للمستخدم على جهازه. يتم ذلك عبر استغلال `File System Access API` الحديثة التي تسمح للمتصفح بالتفاعل مع الملفات بتصريح آمن.

### 🧩 المكونات الجوهرية (Core Components)
1. **Editor Engine (`EditorWrapper`)**: مبني على **CodeMirror 6**، وهو محرك معياري يسمح لنا بإضافة إضافات مخصصة مثل تظليل الماركداون والتحكم في اتجاه النص (RTL/LTR).
2. **Preview Engine (`MarkdownPreview`)**: يستخدم مكتبة `React-Markdown` مع سلسلة من الـ Plugins (`Remark`, `Rehype`) لتحويل النص إلى HTML احترافي.
3. **State Management (`useAppStore`)**: مدارة بواسطة **Zustand**؛ حيث نقوم بتخزين المحتوى، حالة الملف الحالي، إعدادات اتجاه النص، ونسبة التمرير لضمان التزامن.

---

## ⚙️ المنطق البرمجي المتقدم (Advanced Logic)

### 1. ربط المهام التفاعلي (Interactive Tasks Binding)
هذا هو التحدي الأكبر الذي تم حله. التحدي كان في معرفة "أي سطر" في ملف النص المقابل لمربع الاختيار في المعاينة.
- **الحل**: قمت بابتكار نظام `rehype-task-lines`. يقوم هذا الـ Plugin بحقن رقم السطر البرمجي الأصلي داخل فئة الـ CSS (`ClassName`) للمربع.
- عند النقر، نقرأ الفئة، نستخرج الرقم، ونقوم بتعديل السطر المقابل في الـ Store، مما يؤدي لتحديث المحرر فوراً.

### 2. التمرير المتزامن (Bidirectional Scroll Sync)
نستخدم نظام "النسبة المئوية" (`Scroll Ratio`) لضمان تزامن دقيق بين المحرر والمعاينة. يتم حساب نسبة التمرير في العنصر النشط وتطبيقها على العنصر الآخر، مع وجود صمامات أمان (`Refs`) لمنع حلقات التكرار (Infinite Loops).

---

## 🎨 الهوية والتصميم (UI/UX)
- **التصميم**: يعتمد التطبيق على نظام **Shadcn UI** مع تخصيصات لتناسب تجربة الكتابة.
- **الوصولية (Accessibility)**: تم ضبط كافة عناصر ARIA لضمان عمل التطبيق بامتياز مع برامج قراءة الشاشة.
- **الهوية**: الشعار مصمم ليكون واضحاً في المقاسات الصغيرة (`Favicon`) والكبيرة، مع استخدام ألوان HSL مريحة للعين في الأوضاع الفاتحة والداكنة.

---

## 🛠️ سكريبتات العمل (Workflow Scripts)
- `npm run dev`: تشغيل محلي مع دعم HMR.
- `npm run build`: بناء نسخة إنتاج مُحسنة وصغيرة الحجم.
- `deploy.yml`: سير عمل GitHub Actions للنشر التلقائي.

---

<div align="center">
  نُسق ليكون الكود خلف الكلمات بجمال الكلمات نفسها. 💻🖋️
</div>
