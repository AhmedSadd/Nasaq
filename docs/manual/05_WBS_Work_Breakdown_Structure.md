# 📄 Work Breakdown Structure (WBS)
## Project: Nasaq (نسق)
**Version:** 1.1 (Detailed Execution Plan)
**Document ID:** 05_WBS
**Date:** Jan 2026

---

### 1.0 مرحلة التأسيس والبنية التحتية (Foundation & Environment Setup)
**الهدف:** تجهيز بيئة تطوير نظيفة، صارمة، وقابلة للتوسع.

*   **1.1 تهيئة المستودع (Repository Initialization)**
    *   1.1.1 إنشاء مشروع Vite جديد باستخدام قالب React + TypeScript (`npm create vite`).
    *   1.1.2 تنظيف ملفات القالب الافتراضية (Boilerplate Cleanup) وإزالة الـ CSS الافتراضي.
    *   1.1.3 إعداد نظام المسارات (Path Aliases) في `tsconfig.json` و `vite.config.ts` (ربط `@/` بمجلد `src/`).
    *   1.1.4 تهيئة Git وتجهيز ملف `.gitignore` ليشمل ملفات النظام (`.DS_Store`) ومجلدات البناء.
    * 1.1.5 تثبيت vite-plugin-pwa وإعداد manifest.json (لجعل التطبيق قابلاً للتثبيت كبرنامج سطح مكتب).
*   **1.2 معايير جودة الكود (Code Quality Standards)**
    *   1.2.1 تثبيت وإعداد `ESLint` مع قواعد صارمة للـ React Hooks و TypeScript.
    *   1.2.2 تثبيت `Prettier` وتوحيد تنسيق الكود (Single quotes, Trailing commas).
    *   1.2.3 إعداد `Husky` و `lint-staged` لمنع عمل Commit لكود غير منسق أو يحتوي على أخطاء.
    *   1.2.4 إنشاء ملف `.editorconfig` لتوحيد إعدادات المحرر بين فريق العمل.

*   **1.3 مكتبات الواجهة الأساسية (UI Foundation)**
    *   1.3.1 تثبيت `Tailwind CSS` و `PostCSS` و `Autoprefixer`.
    *   1.3.2 تهيئة ملف `tailwind.config.js`:
        *   إضافة دعم الـ Dark Mode عبر الـ Class strategy.
        *   توسيع لوحة الألوان (Color Palette) لتشمل متغيرات CSS الخاصة بـ Shadcn.
        *   إضافة الخطوط العربية (Cairo أو Inter) كخط افتراضي.
    *   1.3.3 تثبيت مكتبة `Shadcn/UI` وتهيئتها (`npx shadcn-ui@latest init`).
    *   1.3.4 تثبيت حزمة الأيقونات `lucide-react`.

*   **1.4 هندسة إدارة الحالة (State Architecture)**
    *   1.4.1 تثبيت مكتبة `Zustand`.
    *   1.4.2 تصميم الـ Store الأولي (`useAppStore`):
        *   تعريف الـ Interfaces (أنواع البيانات).
        *   إنشاء Slices منفصلة (FileSlice, EditorSlice, UISlice) لسهولة الصيانة.
        *   **إضافة حالة `directionMode` من نوع `('auto' | 'rtl' | 'ltr')` إلى `EditorSlice` لتمكين التحكم الشامل في الاتجاه.**
        *   **إضافة منطق لاسترجاع هذه القيمة من `localStorage` عند بدء التشغيل (Persisted Settings).**
    *   1.4.3 إعداد DevTools Middleware لمراقبة تغييرات الحالة أثناء التطوير.

---

### 2.0 مرحلة هيكلية الواجهة والتجربة (UI/UX Layout Phase)
**الهدف:** بناء "الهيكل العظمي" للتطبيق القابل للاستجابة والتفاعل.

*   **2.1 تخطيط الشاشة الرئيسي (Main Layout)**
    *   2.1.1 تثبيت مكتبة `react-resizable-panels`.
    *   2.1.2 بناء هيكل الـ Layout:
        *   Panel 1: الشريط الجانبي (Sidebar) - قابل للطي (Collapsible).
        *   Panel 2: المحرر (Editor) - المنطقة الوسطى.
        *   Panel 3: المعاينة (Preview) - المنطقة اليمنى.
        *   الوضع الافتراضي: Sidebar (Left) -> Editor (Middle) -> Preview (Right).
        *   إضافة خيار `layoutDirection` في `SettingsStore` لتبديل أماكن المحرر والمعاينة (Swap Panes).
    *   2.1.3 إضافة مقابض السحب (Resize Handles) وتنسيقها لتكون مرئية وسهلة الاستخدام.
    *   2.1.4 منطق حفظ أحجام اللوحات في `localStorage` واستعادتها عند إعادة التحميل.

*   **2.2 نظام الثيمات (Theming System)**
    *   2.2.1 تثبيت `next-themes` (أو بناء Hook مخصص) لإدارة تبديل الوضع (Light/Dark).
    *   2.2.2 إنشاء مكون `ThemeToggle` في الشريط الجانبي.
    *   2.2.3 التحقق من عدم وجود "وميض" (FOUC) عند تحميل الصفحة بالوضع الليلي.
    *   2.2.4 تنسيق ألوان الـ Scrollbars لتتوافق مع الثيم المختار.

*   **2.3 مكونات الواجهة المشتركة (Shared Components)**
    *   2.3.1 بناء مكونات Shadcn الأساسية: Button, Dialog, Input, DropdownMenu, Toast.
    *   2.3.2 تخصيص مكون `Toast` (من مكتبة `sonner` أو `radix`) لعرض الإشعارات (نجاح الحفظ، الأخطاء).
    *   2.3.3 تصميم حالات "البيانات الفارغة" (Empty States):
        *   شاشة "Welcome" عند عدم فتح أي ملف.
        *   شاشة "Loading" عند قراءة مجلد كبير.
*   **2.3.4 عناصر التحكم في المحرر (Editor Controls):**
    *   **بناء مكون `DirectionToggleGroup`: مجموعة أزرار (Toggle Group) في شريط الأدوات العلوي للتبديل بين Auto, RTL, LTR.**
    *   **إضافة تلميحات أدوات (Tooltips) تشرح وظيفة كل وضع للمستخدم.**
---

### 3.0 مرحلة محرك المحرر (Core Editor Engine Phase)
**الهدف:** دمج CodeMirror 6 وتطوير المنطق الذكي للكتابة العربية.

*   **3.1 دمج CodeMirror (Basic Integration)**
    *   3.1.1 تثبيت حزم CodeMirror الأساسية (`view`, `state`, `language`, `commands`).
    *   3.1.2 إنشاء مكون `EditorWrapper.tsx`:
        *   استخدام `useRef` لربط المحرر بالـ DOM.
        *   إدارة دورة الحياة (Destroy instance on unmount).
    *   3.1.3 تفعيل حزمة `lang-markdown` ودعم GFM (Jables, Task lists).
    *   3.1.4 تطبيق ثيم CodeMirror ليتوافق مع ألوان Tailwind (Dark/Light).

*   **3.2 منطق إدارة الاتجاه (Direction Logic Engine)**
    *   **3.2.1 هندسة الـ Extension التفاعلي (Reactive Architecture):**
        *   تطوير `Compartment` في CodeMirror للسماح بالتبديل الديناميكي للإضافات (Dynamic Reconfiguration) عند تغيير `directionMode` في الـ Store دون إعادة بناء المحرر بالكامل.
    
    *   **3.2.2 تنفيذ الأوضاع الإجبارية (Forced Modes: RTL/LTR):**
        *   في حالة `forced-rtl` أو `forced-ltr`: يتم تطبيق سمة `dir="rtl"` أو `dir="ltr"` مباشرة على حاوية المحرر (`EditorView.contentAttributes`).
        *   **تحسين:** تعطيل الـ `AutoRTLPlugin` تماماً في هذه الأوضاع لتوفير استهلاك المعالج (CPU).

    *   **3.2.3 تنفيذ الوضع التلقائي (Auto Mode Implementation):**
        *   تطوير `AutoRTLPlugin` كـ `ViewPlugin` يعمل فقط عندما يكون الوضع `auto`.
        *   **الخوارزمية:** كتابة دالة `getDirection` تعتمد على Regex (`^[\u0600-\u06FF]`) لفحص كل سطر.
        *   **الأداء (Optimization):** استخدام `RangeSetBuilder` لتحديث وتلوين الأسطر المرئية فقط (Visible Ranges) وليس كامل المستند.
        *   **حالات الحافة:** التأكد من تجاهل رموز المارك داون (`#`, `>`) عند فحص الحرف الأول للسطر.

    *   **3.2.4 ضبط التنسيقات (CSS & RTL Styling):**
        *   ضبط محاذاة القوائم النقطية (Bullet Points) وعكس المسافات البادئة (Indentation) عند تفعيل كلاس `cm-line-rtl`.
        *   ضمان ظهور مؤشر الكتابة (Cursor) في الاتجاه الصحيح عند تقاطع النصوص العربية والإنجليزية (BiDi Isolation).

*   **3.3 البحث والاستبدال (Search & Replace)**
    *   3.3.1 تفعيل إضافة `search` و `highlightSelectionMatches` من CodeMirror.
    *   3.3.2 تخصيص واجهة لوحة البحث (Search Panel UI) لتشبه تصميم التطبيق (بدلاً من الشكل الافتراضي).
    *   3.3.3 التأكد من دعم البحث للنصوص العربية والتشكيل.

---

### 4.0 مرحلة نظام الملفات والبيانات (File System & Data Phase)
**الهدف:** تنفيذ عمليات الـ CRUD والتعامل مع الـ FSA API والنسخ الاحتياطي.

*   **4.1 محول نظام الملفات (File System Adapter)**
    *   4.1.1 بناء Hook `useFileSystem`:
        *   دالة `openDirectory`: التعامل مع `showDirectoryPicker`.
        *   دالة `readFile`: قراءة النص وفك ترميز UTF-8.
        *   دالة `writeFile`: الكتابة عبر `createWritable`.
    *   4.1.2 إدارة الأذونات (Permission Handling):
        *   دالة `verifyPermission`: للتحقق مما إذا كان الإذن لا يزال ممنوحاً.
        *   معالجة خطأ فقدان الإذن وعرض طلب للمستخدم لإعادة التفعيل.

*   **4.2 عمليات إدارة الملفات (File Operations - CRUD)**
    *   4.2.1 **قراءة (Read):** بناء خوارزمية تكرارية (Recursive Function) لتحويل `DirectoryHandle` إلى شجرة بيانات (Tree Structure) وعرضها في الـ Sidebar.
    *   4.2.2 **إنشاء (Create):** تنفيذ منطق `getFileHandle(name, {create: true})` لإضافة ملفات جديدة.
    *   4.2.3 **حذف (Delete):** تنفيذ منطق `removeEntry` مع رسالة تأكيد (Confirmation Dialog).
    *   4.2.4 **حفظ (Save):**
        *   تطبيق اختصار `Ctrl+S`.
        *   إدارة حالة `isSaving` (Loading Spinner) أثناء الكتابة على القرص.

*   **4.3 التوافقية والنسخ الاحتياطي (Legacy & Backup)**
    *   4.3.1 تثبيت مكتبة `idb-keyval` (للتعامل البسيط مع IndexedDB).
    *   4.3.2 تنفيذ آلية "Firefox Fallback":
        *   تخزين الملفات في الـ Browser Storage بدلاً من القرص.
        *   إنشاء وظيفة "Download as .md" لتصدير العمل.
    *   4.3.3 **حماية البيانات (Data Safety):**
        *   تطبيق منطق `isDirty` (مقارنة المحتوى الحالي مع الأصلي).
        *   إضافة مستمع لحدث `window.onbeforeunload` لمنع الإغلاق العرضي.
---

### 5.0 مرحلة المعاينة المتقدمة (Advanced Preview Phase)
**الهدف:** تحويل نصوص المارك داون إلى وثائق مرئية غنية وتفاعلية.

*   **5.1 معالجة المارك داون (Markdown Rendering)**
    *   5.1.1 تثبيت وتكوين مكتبة `react-markdown` كمكون العرض الأساسي.
    *   5.1.2 دمج الإضافات القياسية (Plugins Integration):
        *   `remark-gfm`: لدعم الجداول، قوائم المهام، والحذف (Strikethrough).
        *   `rehype-highlight`: لتلوين أكواد البرمجة (Syntax Highlighting) داخل كتل العرض (Pre/Code blocks).
        *   `rehype-slug`: لإضافة معرفات (IDs) تلقائية للعناوين (لتمكين روابط الفهرس).
    *   5.1.3 معالجة الصور المحلية (Custom Image Renderer):
        *   اعتراض تاج img.
        *   إذا كان المسار نسبياً (./), استخدام fileHandle لجلب الصورة وتحويلها لـ URL.createObjectURL.
        *   إذا كان المسار مطلقاً (http://...), استخدام URL مباشرة.
    *   5.1.4 إضافة مكون `MarkdownPreview.tsx`:
        *   تطبيق منطق التمرير المتزامن (Sync Scroll).
        *   إضافة مؤشرات تحميل (Loaders/Spinners) عند تحميل الملفات أو رسم الرسوم البيانية.
        *   إضافة تأثيرات Hover وتحديد العناوين (Active Header Highlight).
        *   إضافة إمكانية فتح/إغلاق الفهرس (Table of Contents).
*   **5.2 الرسوم البيانية (Mermaid Diagrams Integration)**
    *   5.2.1 تثبيت مكتبة `mermaid` (إصدار الـ Browser).
    *   5.2.2 إنشاء مكون مخصص `MermaidBlock.tsx`:
        *   اعتراض كتل الكود (Code Blocks) المعرفة بـ `mermaid`.
        *   استخراج النص الخام (Raw Text) وتمريره لمحرك Mermaid.
        *   التعامل مع الأخطاء (Error Handling): إذا كان كود الرسم غير صالح، عرض رسالة خطأ لطيفة بدلاً من تحطم الواجهة.
        *   تحديث الرسم تلقائياً عند تغيير الكود (Re-render logic).

*   **5.3 الأمان والتعقيم (Security & Sanitization)**
    *   5.3.1 تثبيت `rehype-sanitize` وتكوينه.
    *   5.3.2 تعريف القائمة البيضاء (Allowlist): السماح فقط بالتاجات الآمنة (h1-h6, p, ul, table, img...) ومنع السكربتات (`script`, `iframe`, `object`).
    *   5.3.3 منع تحميل الصور من مصادر خارجية غير موثوقة (اختياري: تقييد الـ `src` ليكون محلياً أو HTTPS فقط).

*   **5.4 التزامن (Sync Scroll)**
    *   5.4.1 تطبيق منطق التمرير المتزامن:
        *   حساب نسبة التمرير في المحرر (Scroll Percentage).
        *   تطبيق نفس النسبة على حاوية المعاينة (Preview Container).
        *   (تحسين متقدم): استخدام "Mapping" دقيق لربط كل سطر في المحرر بالعنصر المقابل له في الـ HTML (بإضافة `data-line` attributes) لضمان تزامن دقيق حتى مع الصور والرسوم البيانية.

---

### 6.0 مرحلة تحسين الإنتاجية (Productivity & UX Polish Phase)
**الهدف:** تحويل التطبيق من مجرد "محرر" إلى "بيئة عمل متكاملة" سلسة الاستخدام.

*   **6.1 إدارة الاختصارات (Keyboard Shortcuts Manager)**
    *   6.1.1 تثبيت `react-hotkeys-hook`.
    *   6.1.2 تعريف خريطة الاختصارات (Shortcuts Map):
        *   `Mod+S`: حفظ الملف (Save).
        *   `Mod+B`: إظهار/إخفاء الشريط الجانبي (Toggle Sidebar).
        *   `Mod+Shift+P`: (مستقبلاً) فتح لوحة الأوامر.
        *   `F11` (أو اختصار مخصص): وضع ملء الشاشة.
        *   **`Alt + Shift + D`: تدوير وضع اتجاه النص (Cycle Text Direction: Auto -> RTL -> LTR).**
    *   6.1.3 التأكد من عدم تعارض الاختصارات مع اختصارات المتصفح الافتراضية الحرجة.

*   **6.2 وضع التركيز (Zen Mode)**
    *   6.2.1 إضافة حالة `isZenMode` في `UISlice` (Zustand).
    *   6.2.2 تطبيق فئات CSS مشروطة: عند التفعيل، يتم إخفاء الـ Sidebar والـ Header، وتوسيع المحرر ليملأ الشاشة (أو توسيطه بعرض مريح للقراءة).
    *   6.2.3 إضافة زر عائم (Floating Action Button) أو اختصار للخروج من وضع التركيز.

*   **6.3 التحسينات البصرية الدقيقة (Micro-Interactions)**
    *   6.3.1 إضافة مؤشرات تحميل (Loaders/Spinners) عند القيام بعمليات ثقيلة (مثل فتح مجلد كبير).
    *   6.3.2 تحسين تصميم "شجرة الملفات":
        *   إضافة أيقونات مميزة لأنواع الملفات (`.md`, `.json`, `.js`).
        *   إضافة تأثير Hover وتحديد الملف النشط (Active File Highlight).
        *   (اختياري) إضافة إمكانية فتح/إغلاق المجلدات (Collapsible Directories).

---

### 7.0 مرحلة الاختبار وضمان الجودة (QA & Testing Phase)
**الهدف:** التأكد من استقرار النظام وخلوه من الأخطاء الحرجة قبل النشر.

*   **7.1 اختبارات الوحدات (Unit Testing)**
    *   7.1.1 تثبيت `Vitest` و `React Testing Library`.
    *   7.1.2 كتابة اختبارات لمنطق `AutoRTL`:
        *   Test Case: نص عربي خالص -> `rtl`.
        *   Test Case: نص إنجليزي -> `ltr`.
        *   Test Case: نص مختلط (يبدأ بعربي) -> `rtl`.
        *   Test Case: نص يبدأ برمز Markdown (`# عنوان`) -> `rtl`.

*   **7.2 اختبارات التكامل (Integration Testing)**
    *   7.2.1 اختبار دورة حياة الملف (Mock File System):
        *   إنشاء ملف افتراضي في الذاكرة -> فتحه في المحرر -> تعديله -> حفظه -> التأكد من تغير المحتوى في الذاكرة.

*   **7.3 اختبار التوافقية اليدوي (Manual Cross-Browser Testing)**
    *   7.3.1 **Chrome/Edge:** التأكد من عمل FSA API والحفظ المباشر.
    *   7.3.2 **Firefox:** التأكد من ظهور وضع الـ Fallback (اختفاء زر Open Folder، عمل زر Download).
    *   7.3.3 **Safari:** التحقق من التوافق (غالباً سيعامل معاملة Firefox).

---

### 8.0 مرحلة النشر والتسليم (Deployment & Handover Phase)
**الهدف:** إطلاق التطبيق للمستخدمين وتوفير التوثيق النهائي.

*   **8.1 إعداد البناء (Build Configuration)**
    *   8.1.1 تحسين إعدادات Vite Build:
        *   تفعيل تقسيم الكود (Code Splitting) لتقليل حجم التحميل الأولي (Lazy loading for Mermaid & Preview).
        *   التأكد من ضغط الأصول (Assets Minification).
    *   8.1.2 حل مشاكل المسارات (Base Path Issue) إذا كان النشر سيتم في مجلد فرعي.

*   **8.2 النشر على GitHub Pages**
    *   8.2.1 إنشاء ملف `deploy.yml` (GitHub Actions Workflow):
        *   Trigger: عند الدفع لفرع `main`.
        *   Steps: Checkout -> Install -> Build -> Deploy to `gh-pages` branch.
    *   8.2.2 إضافة ملف `.nojekyll` في مجلد الـ `public` لمنع تجاهل الملفات التي تبدأ بـ `_`.

*   **8.3 التوثيق النهائي (Final Documentation)**
    *   8.3.1 كتابة ملف `README.md` احترافي:
        *   وصف المشروع.
        *   المميزات (Screenshots/GIFs).
        *   كيفية التشغيل محلياً (Installation).
    *   8.3.2 (اختياري) كتابة دليل المساهمة `CONTRIBUTING.md` إذا كان المشروع مفتوح المصدر.

---

### ✅ معايير انتهاء المشروع (Project Completion Definition)
يعتبر المشروع مكتملاً وجاهزاً للتسليم عند تحقق الشروط التالية:
1.  جميع التذاكر (Tickets) في حالة "Done".
2.  لا توجد أخطاء حرجة (Critical Bugs) تمنع الوظائف الأساسية (الفتح، التحرير، الحفظ).
3.  التطبيق يعمل بنجاح على GitHub Pages.
4.  اختبار الـ Auto-RTL ينجح في التعامل مع النصوص المختلطة بشكل صحيح.